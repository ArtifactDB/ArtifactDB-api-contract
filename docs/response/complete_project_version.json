{
    "$id": "complete_project_version.json",
    "$schema": "http://json-schema.org/draft-07/schema",
    "description": "JSON body of the response for the `PUT /projects/{project}/version/{version}/complete` endpoint.",
    "properties": {
        "job_id": {
            "description": "Identifier of the job to be used in the `GET /jobs/{jobid}` endpoint, to determine the success of asynchronous post-upload tasks such as indexing.",
            "type": [
                "integer",
                "string"
            ]
        }
    },
    "required": [
        "job_id"
    ],
    "title": "Complete project version response",
    "type": "object"
}
